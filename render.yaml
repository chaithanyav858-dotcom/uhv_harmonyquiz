services:
  - type: web
    name: harmony-quiz-game
    env: node
    branch: main
    plan: starter
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
